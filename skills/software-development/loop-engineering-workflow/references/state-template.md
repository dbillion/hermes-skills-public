# Loop State — <project name>
Last run: <YYYY-MM-DD>
Project: <one-line description, e.g. NestJS 11 + Angular 20 on Neon>

## DONE + VERIFIED (real tool output, committed)
- <capability> — <how verified: curl/build/DB query> (commit <sha>)
- <capability> — <how verified> (commit <sha>)

## PENDING SLICES (L2: act + verify, then commit)
- [ ] <slice id>: <what> — <acceptance: how you'll verify>
- [ ] <slice id>: <what> — <acceptance>

## BLOCKED / DEFERRED
- <item>: <why> (e.g. external worker down, missing key) — note fallback shipped
